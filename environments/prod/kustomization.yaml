apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../apps/project-a/base

nameSuffix: -prod

replicas:
  - name: api
    count: 3

images:
  - name: yourrepo/api
    newTag: prod


apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../apps/project-a/base

nameSuffix: -stage

replicas:
  - name: api
    count: 2

images:
  - name: yourrepo/api
    newTag: stage

